{
  "name": "nim-game-2",
  "version": "1.0.0",
  "description": "a simple name game using websockets.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "vite build && mv dist client/dist", // Added command to move the build output to client/dist
    "start": "node server/index.js"
  },
  "keywords": [
    "heroku"
  ],
  "author": "jane-doe",
  "license": "MIT",
  "dependencies": {
    "express": "^4.9.8",
    "socket.io": "^4.0.1",
    "vite": "^2.3.8"
  },
  "engines": {
    "node": "20.x"
  }
}
