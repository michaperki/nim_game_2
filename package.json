{
  "name": "nim-game-2",
  "version": "1.0.0",
  "description": "a simple name game using websockets.",
  "main": "server/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && npm install && npm run build",
    "start": "node server/index.js"
  },
  "keywords": [
    "heroku"
  ],
  "author": "jane-doe",
  "license": "MIT",
  "dependencies": {
    "express": "^4.9.8",
    "socket.io": "^4.0.1",
    "vite": "^3.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^2.2.0"
  },
  "engines": {
    "node": "20.x"
  }
}
